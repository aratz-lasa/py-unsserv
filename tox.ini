[tox]
skipsdist=True
envlist = lint,py37


[flake8]
max-line-length = 88

[testenv]
deps =
    -rrequirements.txt
    -rdev_requirements.txt

commands = pytest --cov-report term --cov=unsserv tests/


[testenv:lint]
deps = -rdev_requirements.txt
commands =
    python -m flake8 {toxinidir}/unsserv {toxinidir}/tests
    python -m mypy --no-strict-optional --ignore-missing-imports {toxinidir}/unsserv
    python -m black --check unsserv tests
    python -m isort --recursive --check-only --diff {toxinidir}/unsserv {toxinidir}/tests
    python -m docformatter --pre-summary-newline --check --recursive unsserv tests

